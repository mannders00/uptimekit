route:
  receiver: visible-in-ui
  group_by: [alertname, environment]
  group_wait: 15s
  group_interval: 1m
  repeat_interval: 4h
receivers:
  - name: visible-in-ui
# Intentionally no outbound receiver while the operator has email disabled.
# Alert state remains available to Grafana and the curated public UI.
